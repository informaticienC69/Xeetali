# react-simple-maps@3 déclare une plage peer React périmée (^16/17/18) mais
# fonctionne avec React 19. On aligne l'install locale sur le Dockerfile
# (npm install --legacy-peer-deps) pour que `npm install` / `npm run dev` passent.
legacy-peer-deps=true
